using UnityEngine;

/// <summary>
/// Obsolete — the key bindings panel is now built by HotkeyRebindUI (UI Toolkit).
/// This component is kept as a no-op so existing scene references don't cause compile errors.
/// You can safely remove it from any GameObjects in the scene.
/// </summary>
public class HotkeyPanelBuilder : MonoBehaviour { }
